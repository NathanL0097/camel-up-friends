# 好友桌游馆：长期项目上下文

## 项目是什么

好友桌游馆是一个面向熟人好友房的中文在线桌游平台。平台统一提供游戏目录、创建和加入房间、邀请链接、玩家身份、断线重连及实时行动分发；每款游戏分别实现服务端规则、公开状态和浏览器界面。

项目采用可扩展的多游戏结构。新增游戏不应复制大厅、房间或重连逻辑，而应接入统一平台能力。

## 技术栈

- Node.js 20+，运行时使用 CommonJS JavaScript。
- Express 5 提供 HTTP、静态资源和管理接口。
- Socket.IO 4 提供实时房间通信。
- 原生 HTML、CSS 和 JavaScript；没有前端框架或常规构建步骤。
- PostgreSQL（`pg`，可连接 Neon）用于需要持久化的数据。
- Node.js 内置测试框架用于规则和平台测试。
- Web App Manifest 与 Service Worker 提供 PWA 能力。
- 少量 TypeScript 文件用于协议和类型说明，不是主要运行时。

## 核心架构

1. `server.js` 是 HTTP、Socket.IO、权限、后台、定时器及数据库接入层。
2. `src/platform/room-service.js` 负责通用好友房、开局、再开一局、配置和动作分发。
3. `src/games/index.js` 是服务端游戏注册表。
4. 每个游戏模块拥有独立的元数据、服务端权威规则、动作处理器和按玩家裁剪的公开状态。
5. 浏览器通过游戏元数据中的 `clientScript` 动态加载对应客户端模块。
6. 服务端修改唯一权威状态，然后为每个观看者分别生成安全的公开状态并发送 `room:update`。
7. 定时游戏由统一 ticker 推进；需要人类确认的阶段通过游戏自己的确认动作控制。

## 关键目录和文件

- `server.js`：应用入口和网络接入层。
- `src/platform/`：房间、连接状态、权限和数据库服务。
- `src/games/index.js`：当前有效游戏注册表。
- `src/games/<game-id>/index.js`：游戏元数据、配置和动作映射。
- `src/games/<game-id>/rules.js`：服务端规则及 `publicRoom` 状态裁剪。
- `public/app.js`：大厅、玩家身份、邀请、重连和客户端模块加载。
- `public/games/`：各游戏的浏览器界面、交互和动画。
- `public/styles.css`：平台级视觉样式。
- `public/game-ui-system.css`、`public/product-quality.css`：共享交互基线与各游戏专属视觉层。
- `public/assets/`、`public/game-art.js`：原创场景与游戏符号。
- `public/platform-audio.js`、`public/game-moments.js`：呈现层声音与公开事件演出；不得参与规则计算。
- `public/room-identities.js`：按房间保存本设备重连身份，不得发送到公开状态。
- `test/`：平台及游戏自动测试。

## 多人联机原则

- 服务端是规则和随机结果的唯一权威来源；客户端只提交意图并渲染结果。
- 所有行动必须在服务端校验玩家、回合、阶段、目标和资源是否合法。
- `publicRoom(room, viewerId)` 必须按观看者裁剪信息；隐藏身份、手牌、骰子、预测、资产和重连令牌不能泄露。
- 同一玩家可以有多个连接；只有最后一个连接断开时才标记离线。
- 刷新或短暂断线后，玩家应通过本地身份令牌回到原座位。
- 新游戏统一使用 `game:action`；你画我猜的增量笔迹广播是受控的专用通道。
- 重要结果不能在玩家来不及理解时自动跳过。骰子、派彩、揭示、规则和文字密集事件应按内容保留足够阅读时间，必要时使用暂停或手动确认。

## 不允许破坏的功能

- 不得泄露玩家令牌或任何只属于特定玩家的信息。
- 不得破坏房间码、邀请链接、房间密码、房主权限、刷新重连、多连接状态或再开一局。
- 不得让客户端绕过服务端规则校验或自行计算权威结果。
- 不得把你画我猜的 `draw` 笔迹通道误用于其他游戏的同名动作。
- 不得缩短关键动画和结果展示到无法正常观察、阅读和理解。
- 保持各游戏的确认和断线恢复语义。列车普通结果采用有界阅读停留后自动继续；重大简报允许在线玩家全员确认后提前继续，不能永久等待离线或忘记确认的玩家。
- 移动端必须保持可操作；视觉改造不能以破坏触控、响应式布局或私密信息区为代价。
- 自动测试必须继续全部通过，同时视觉与游戏手感还必须通过真实浏览器渲染验证。
- 所有游戏取消自动语音通知与语音播报开关；保留非人声游戏音效及全局静音、音量控制。不要删除供屏幕阅读器使用的可访问性语义。

## 游戏注册方式

1. 在 `src/games/<game-id>/` 实现游戏定义和服务端规则。
2. 定义稳定的 `id`、标题、描述、人数范围、成熟度、`clientScript`、`createGame`、`publicRoom` 和 `actions`；需要时增加开局配置、ticker 与重连钩子。
3. 在 `src/games/index.js` 注册定义。只有该注册表中的游戏属于当前产品目录。
4. 在 `public/games/<game-id>.js` 注册同名客户端工厂，并通过公共平台接口发送动作和渲染状态。
5. 添加对应样式与自动测试，重点验证完整游戏流程、非法动作、秘密信息裁剪、断线情况和移动端关键结构。
6. 除非明确执行迁移，不得删除现有骆驼旧动作兼容入口。
